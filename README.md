main: 主分支 
1. 存放可运行、无错误的代码 / 文件；
2. 禁止直接在 main 分支修改 / 提交 
3. 仅接收临时分支的合并，合并前必须验证无误；
4. 永久保留，全程作为基准

dev-xxx：临时分支（按需创建）
1. 命名规范：dev-member-feature（如dev-ljh-code、dev-srb-model）；
2. 基于最新的 main 分支创建，每人可创建自己的临时分支；
3. 功能完成 / 验证无误后，合并回 main；
4. 临时分支可推送到远程，便于队友协助调试

.gitignore 为配置文件，规定不受Git监视的文件类型（通常为临时文件或辅助文件）

所有队员必须遵循 **「拉取→开发→提交→推送→合并」** 的流程，核心原则：先拉取（pull）再推送（push），这是减少合并冲突的关键，竞赛中 90% 的 Git 问题都是因为忽略了这一步。

为方便团队其他成员，提交更改时message尽可能描述地清楚具体一些
